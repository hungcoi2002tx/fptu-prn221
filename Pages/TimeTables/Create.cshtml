@page
@model Assignment.Pages.TimeTables.CreateModel
@{
}
<h4>Create new schedule</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EditModel.TeacherCode" class="control-label"></label>
                <select asp-for="EditModel.TeacherCode">
                    @foreach (var item in Model.Teachers)
                    {
                        <option value="@item.Code">@item.Name</option>
                    }
                </select>
                <span asp-validation-for="EditModel.TeacherCode" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                <label asp-for="Teacher.Code" class="control-label"></label>
                <input asp-for="Teacher.Code" class="form-control" />
                <span asp-validation-for="Teacher.Code" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
            <input asp-for="Teacher.Id" value=" " hidden="true" />*@
        </form>
    </div>
</div>

<div>
    <button class="back-list-btn">
        <a asp-page="Index">Back to List</a>
    </button>
</div>