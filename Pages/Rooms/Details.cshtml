@page
@model Assignment.Pages.Rooms.DetailsModel

<div>
    <h4>Room</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Room.Code)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Room.Code)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Room.CreateTime)
        </dt>
        <dd class="col-sm-10">
            @Model.Room.CreateTime.Value.ToString("dd/MM/yyyy")
        </dd>
    </dl>
</div>
<div>
    <button class="edit-btn">
        <a asp-page="./Edit" asp-route-id="@Model.Room?.Id">Edit</a>
    </button>    
    <button class="back-list-btn">
        <a asp-page="./Index">Back to List</a>
    </button>
</div>
