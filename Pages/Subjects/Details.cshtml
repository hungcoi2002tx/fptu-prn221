@page
@model Assignment.Pages.Subjects.DetailsModel

<div>
    <h4>Subject</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Subject.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Subject.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Subject.Code)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Subject.Code)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Subject.CreateTime)
        </dt>
        <dd class="col-sm-10">
            @Model.Subject.CreateTime.Value.ToString("dd/MM/yyyy")
        </dd>
    </dl>
</div>
<div>
    <button class="edit-btn">
        <a asp-page="./Edit" asp-route-id="@Model.Subject?.Id">Edit</a>
    </button>
    <button class="back-list-btn">
        <a asp-page="./Index">Back to List</a>
    </button>
    
</div>
